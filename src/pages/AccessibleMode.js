import React from 'react';

const AccessibleMode = () => {
  return (
    <div style={styles.page}>
      <h1>Accessible Mode</h1>
      <p>Enable accessible mode for enhanced usability.</p>
    </div>
  );
};

const styles = {
  page: {
    padding: '20px',
    backgroundColor: '#f8f9fa',
    fontFamily: 'Arial, sans-serif',
  },
};

export default AccessibleMode;
